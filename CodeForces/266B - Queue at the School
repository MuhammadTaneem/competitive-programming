#include <bits/stdc++.h>
using namespace std;
#define all(x) (x).begin(),(x).end()
#define endl "\n"
#define fr(x) for(int i=1;i<x;i++)
#define frj(x) for(int j=0;j<x;j++)
int nxt()
{
    int x;
    cin>>x;
    return x;
}
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int g=0,b=0,t=nxt(),c=nxt();
    string s;
    cin>>s;
    set<int>v;
    while(c--)
    {
         fr(t){
        if(s[i]=='G'&&s[i-1]=='B')
        {
            swap(s[i],s[i-1]);
            ++i;
        }
    }
    }

    cout<<s<<endl;
    return 0;
}
