#include <bits/stdc++.h>
using namespace std;
 
int main()
{
    int a,c=0;
    scanf("%d",&a);
    while(a--)
    {
        int f,s,t;
        scanf("%d%d%d",&f,&s,&t);
        if(f+s+t>1)
        {
            c++;
        }
    }
    printf("%d\n",c);
    return 0;
}
 
