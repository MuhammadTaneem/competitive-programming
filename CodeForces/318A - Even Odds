#include <bits/stdc++.h>
using namespace std;
 
int main()
{
    long long int a,b;
    scanf("%lld%lld",&a,&b);
    if(a&1)a++;
    if(b<=(a/2))
    {
        printf("%lld\n",(2*b)-1);
        return 0;
    }
    printf("%lld\n",(2*b)-a);
    return 0;
}
