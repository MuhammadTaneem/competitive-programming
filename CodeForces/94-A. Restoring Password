#include <bits/stdc++.h>
using namespace std;

int main()
{
    string a;
    cin>>a;
    string s[8];
    s[0]=a.substr(0,10);
    s[1]=a.substr(10,10);
    s[2]=a.substr(20,10);
    s[3]=a.substr(30,10);
    s[4]=a.substr(40,10);
    s[5]=a.substr(50,10);
    s[6]=a.substr(60,10);
    s[7]=a.substr(70,10);

    string v[10];
    cin>>v[0]>>v[1]>>v[2]>>v[3]>>v[4]>>v[5]>>v[6]>>v[7]>>v[8]>>v[9];
    int n=0;
    while(n<8)
    {
        for(int i=0;i<10;i++)
        {
            if(s[n]==v[i])
            {
                printf("%d",i);
            }
        }
        n++;
    }
    printf("\n");
    return 0;
}

