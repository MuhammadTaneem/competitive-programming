#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    scanf("%d",&n);
    while(n>0)
    {
        int h,m;
        scanf("%d%d",&h,&m);
        printf("%d\n",(1440-((h*60)+m)));
        n--;
    }
    return 0;
}

