#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    scanf("%d",&n);
    while(n>0)
    {
        int c,k,t;
        scanf("%d%d",&c,&k);
        t=c-(c%k);
        t+=min(c%k,k/2);
        printf("%d\n",t);
        n--;
    }
    return 0;
}

