#include <bits/stdc++.h>
 
using namespace std;
 
int main()
{
 
 
	int n,s=0;
	cin >> n;
	vector<int>arr(n);
	for (int i=0;i<n;++i){
		cin >> arr[i];
	}
	sort(arr.begin(),arr.end());
	for (int i=0;i<n;i++) {
		if (arr[i]>s)
            s++;
	}
	cout <<s<< endl;
	return 0;
}
