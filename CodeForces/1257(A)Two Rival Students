#include <iostream>
using namespace std;
 
int main() {
	int q;
	cin >> q;
	cin.ignore();
	for (int qr=0;qr<q;qr++)
    {
		long long int a,b,n,s;
		cin>>a>>b>>n>>s;
		if (s % n <= b&&(a*n)+b>=s)
        {
			cout << "YES"<<endl;
		}
		else
        {
			cout << "NO"<<endl;
		}
	}
	return 0;
}
