#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    scanf("%d",&n);
    while(n>0)
    {
        int strs;
        string str;
        cin>>str;
        strs=str.size();
        if((str[strs-2]=='p')&&(str[strs-1]=='o'))
        {
          printf("FILIPINO\n");
        }
        else if((str[strs-5]=='m')&&(str[strs-4]=='n')&&(str[strs-3]=='i')&&(str[strs-2]=='d')&&(str[strs-1]=='a'))
        {
          printf("KOREAN\n");
        }
        else{
            printf("JAPANESE\n");
        }
        n--;
    }
    return 0;
}

